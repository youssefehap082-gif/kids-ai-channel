name: "Shorts Auto Upload (6x)"

on:
  schedule:
    - cron: "0 */4 * * *"  # كل 4 ساعات
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Start Workflow
        run: echo "🎬 Starting Shorts Auto Upload..."

      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install google-api-python-client google-auth google-auth-oauthlib google-auth-httplib2 ffmpeg-python numpy opencv-python-headless

      - name: Install FFmpeg
        run: sudo apt-get install -y ffmpeg

      - name: 🧠 Generate & Upload Shorts
        run: |
          echo "🎥 Running Shorts Script..."
          cd src
          python main_shorts.py || exit 1
        env:
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN }}
