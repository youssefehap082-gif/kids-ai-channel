name: Daily Shorts (6x)

on:
  schedule:
    # تشغيل 6 مرات يوميًا بفواصل كل 4 ساعات
    - cron: '0 1 * * *'
    - cron: '0 5 * * *'
    - cron: '0 9 * * *'
    - cron: '0 13 * * *'
    - cron: '0 17 * * *'
    - cron: '0 21 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set up job
        run: echo "🎥 Starting Daily Shorts job..."

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install FFmpeg
        run: sudo apt-get install -y ffmpeg

      - name: Generate & upload short videos
        run: |
          echo "🎬 Generating short videos..."
          for i in 1 2 3 4 5 6; do
            python src/main_shorts.py || echo "⚠️ Upload failed, retrying in 2 hours..."
            sleep 5
          done
