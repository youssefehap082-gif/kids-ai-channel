name: AI Optimizer (daily)

on:
  schedule:
    # تشغيل مرة واحدة يوميًا الساعة 3 صباحًا بتوقيت مصر
    - cron: '0 1 * * *'
  workflow_dispatch:

jobs:
  optimize:
    runs-on: ubuntu-latest
    steps:
      - name: Set up job
        run: echo "🧠 Starting AI Optimizer..."

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Optimizer AI
        run: |
          echo "🔍 Running AI optimizer..."
          python src/optimizer_ai.py
